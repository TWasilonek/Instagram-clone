import Posts from "./Posts";
import Stories from "./Stories";

const Feed = () => {
  return (
    <main className=" ">
      <section className="">
        <Stories />
        <Posts />
      </section>

      <section className="">
        column
        {/* Mini Profile */}
        {/* Suggestions */}
      </section>
    </main>
  );
};

export default Feed;
